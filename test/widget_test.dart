import 'package:flutter_test/flutter_test.dart';
import 'package:windyomi/main.dart';

void main() {
  test('Windyomi app widget can be constructed', () {
    expect(const MyApp(), isA<MyApp>());
  });
}
